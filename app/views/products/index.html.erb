<% if !@products.present? %>
  <p>There are no posts containing the term(s) <%= params[:search] %>.</p>
<% end %>

<%= render partial: "shared/products", locals:
  {
    sorted: @products,
    title: "All Products"
  } %>
