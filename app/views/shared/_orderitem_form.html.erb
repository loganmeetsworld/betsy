<% if @product.stock != 0 %>  
  <%= form_for @order_item, url: {controller: :orderitems, action: :add_to_cart, method: :patch}, class: "form-group" do |f| %>
    <h5><%= @product.stock %> in stock</h5>
    <%= f.label :quantity %>
    <%= f.number_field :quantity, class: "quantity", :value => 1 %>
    <% f.hidden_field :product_id, :value => @product.id %>
    <%= f.submit "Add to Cart", class: "btn btn-default" %>
  <% end %>
<% end %>