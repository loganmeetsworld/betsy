<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Botsy", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "Sell on Botsy" %></li>
        <li><%= link_to "" %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Browse<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "All Products", products_path %> </li>
            <li role="separator" class="divider"></li>
            <li class="dropdown-header">By Category</li>
            <% @categories.each do |category| %> 
              <li><% link_to category.name, category_path(category.name) %></li>
            <% end %>
            <li class="dropdown-header">By Robot</li>
            <% @robots.each do |robot| %> 
              <li><% link_to robot.name, robot_products_path(robot) %></li>
            <% end %>
          </ul>

          <ul class="nav navbar-nav navbar-right">
          <% if current_user.nil? %> 
            <li>Login</li>
            <li>Signup</li>
          <% else %> 
            <li>Logout</li>
            <li>Account Info</li>
          <% end %> 
        <li class="navbar-text cart-number"><%= current_order.orderitems.count %></li>
        <li class="navbar-link"><i class="icon-shopping-cart"></i> icon-shopping-cart<%= link_to " My Cart", orders_path %></li>
      </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>