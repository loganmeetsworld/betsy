<nav>
  <div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#myNavbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Botsy", root_path, class: "navbar-brand" %>
      </div>
      <a name="#top"></a>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Browse<b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to "All Products", products_path %> </li>
              <li role="separator" class="divider"></li>
              <li class="dropdown-header">By Category</li>
              <% @categories.each do |category| %>
                <li><%= link_to category.name, by_category_path(category.name) %></li>
              <% end %>
              <li role="separator" class="divider"></li>
              <li class="dropdown-header">By Robot</li>
              <% @robots.each do |robot| %>
                <li><%= link_to robot.name, robot_products_path(robot) %></li>
              <% end %>
              <li role="separator" class="divider"></li>
            </ul>
          </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <% if @robot.nil? %>
            <li><%= link_to "Robot Login" %></li>
            <li><%= link_to "Sell on Botsy" %></li>
          <% else %>
           <li><%= link_to "Logout" %></li>
           <li><%= link_to "Account Info" %></li>
          <% end %>
          <li class="navbar-text"><%= @current_order.total_items %>
          <li class="navbar-link"><%= link_to "", orders_path, class: "glyphicon glyphicon-shopping-cart cart" %></li></li>
          <li class="navbar-text">  </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
